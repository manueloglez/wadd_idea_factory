<%= form_with(model: @idea, local: true) do |form| %>
  <div>
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>
  <div>
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>
  <%= form.submit(@idea.new_record? ? 'Create' : 'Update') %>

<% end %>